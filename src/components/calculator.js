import React from "react"
import styled from "styled-components"
import { theme } from "@styles"
const { colors } = theme

const StyledCalculator = styled`
  background: linear-gradient(#b1b9bd 35%, ${colors.nord5} 0);
  display: block;
  position: relative;
  top: 7rem;
  margin: auto;
  border-radius: 3rem 3rem 3rem 3rem;
  height: 70vh;
  width: 70vw;
  padding: 0.5rem;
  box-shadow: 15px -15px #2e3440;
`

const Calculator = () => {}
export default Calculator
